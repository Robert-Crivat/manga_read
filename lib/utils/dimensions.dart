import 'package:flutter/material.dart';

/// Contiene le dimensioni degli elementi della UI

/// Padding del container antenato
const double outerPadding = 16.0;

/// Border Radius del container antenato
const double outerBorderRadius = 18.0;

/// Altezza del container visualizzato durante il loading delle informazioni
const double defaultLoadingContainerHeight = 300.0;

/// Dimensione delle icone di interazione
const double iconDimension = 24.0;

/// Separazione standard tra gli elementi della UI
const double defaultSeparation = 16.0;

const double cardBorderRadius = 16.0;
const double cardPadding = 16.0;

const Widget hGap = SizedBox(height: 16);
const Widget hGapSmall = SizedBox(height: 8);
const Widget hGapMini = SizedBox(height: 4);
const Widget wGap = SizedBox(width: 16);
const Widget wGapSmall = SizedBox(width: 8);
const Widget wGapMini = SizedBox(width: 4);
